[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "connectors"
version = "0.1.0"
description = "Data-source connectors: scrapers, feeds and messaging."
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "Robert van Kommer", email = "robert.vankommer@gmail.com" }]
keywords = ["connectors", "scraping", "feeds"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
# connectors.grounding only. It also needs the sibling local libraries
# `vectorize` (embeddings + Qdrant access) and `matching_learning`
# (fuzzy/vector duplicate detection), which are installed from the workspace
# rather than from PyPI.
grounding = ["qdrant-client>=1.7", "pydantic>=2"]
# connectors.dataspace only: the collector verifies URLs over HTTP and runs its
# searched source on Gemini. The crawled source additionally uses
# connectors.scraper's Crawl4AIScraper, imported lazily so a host without
# crawl4ai still collects from the other two sources.
dataspace = ["requests>=2.28", "pydantic>=2", "google-genai>=1.0", "python-dotenv>=1.0"]

[project.urls]
Homepage = "https://github.com/kiodia/connectors"
Repository = "https://github.com/kiodia/connectors"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
